{
  "name": "newtrip-v1",
  "version": "0.1.0",
  "private": true,
  "description": "NewTrip / Travel Simulator V1 playable spine workspace.",
  "workspaces": [
    "apps/backend"
  ],
  "scripts": {
    "dev": "npm run dev -w @newtrip/backend",
    "build": "npm run build -w @newtrip/backend",
    "test": "npm run test -w @newtrip/backend",
    "test:e2e": "npm run test:e2e -w @newtrip/backend",
    "db:smoke": "npm run db:smoke -w @newtrip/backend",
    "config:validate": "npm run config:validate -w @newtrip/backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
